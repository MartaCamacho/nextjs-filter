// Shared by both exercises. The layout (back link + heading) stays visible;
// only the data-dependent range area shows this skeleton while the page streams.
const Loading = () => {
  return <div className="h-11 w-full animate-pulse bg-neutral-100" />;
};

export default Loading;
